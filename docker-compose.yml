services:
  auth-app:
    build: .
    container_name: auth-app
    volumes:
      - auth-app-data:/data
    environment:
      - DATABASE_PATH=/data/users.db
      - FLASK_ENV=production
      - SECRET_KEY=AABBCCDDEEFFGG
    ports:
      - "7906:5000"  # Flask app - Expuesto para NPM
    restart: unless-stopped
volumes:
  auth-app-data:

